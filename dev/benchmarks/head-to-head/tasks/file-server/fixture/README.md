# Local file server

Run `node src/cli.js DIRECTORY` and use the reported loopback port.
`npm test` checks basic file serving. Conditional requests, byte ranges and
path validation still need the behavior described in the requested task.
