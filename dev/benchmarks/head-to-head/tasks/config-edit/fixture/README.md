# Configuration edits

A small JSON editing library and command-line wrapper. Run `npm test` for the
existing smoke tests. The migration implementation currently handles basic set
operations only and needs the requested validation and rollback behavior.
